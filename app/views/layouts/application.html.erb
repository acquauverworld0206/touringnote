<!DOCTYPE html>
<html>
  <head>
    <title>Touring Note</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  </head>

  <%# トップページではダークテーマ、それ以外はライトテーマを適用 %>
  <%# トップページとDevise関連ページではダークテーマを適用 %>
  <body class="min-h-screen">
    <%= render 'layouts/header' %>
    
    <% if current_page?(root_path) || devise_controller? %>
      <main class="relative flex items-center justify-center p-4 pt-24" style="background-color: #0d1117; min-height: 100vh;">
        <%= yield %>
      </main>
    <% else %>
      <main class="relative flex flex-col items-center justify-start p-4 pt-24 bg-gray-100" style="min-height: 100vh;">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
